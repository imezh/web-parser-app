# Web Parser - Парсер веб-страниц для Windows

**Версия 1.2.0** - Улучшенный автоматический выбор сертификатов

Консольное приложение для парсинга веб-страниц с поддержкой:
- ✅ **Полностью автоматического выбора клиентских сертификатов** из хранилища Windows (без диалогов)
- ✅ Ожидания полной загрузки динамических страниц
- ✅ Работы со сложными веб-приложениями (JavaScript, AJAX)
- ✅ Профессионального логирования (5 уровней, автоматическая ротация)
- ✅ Вывода результатов в формате JSON

## Возможности

- **Автоматический выбор сертификатов**: Приложение полностью автоматически использует клиентские сертификаты из хранилища Windows без появления диалоговых окон
- **Умное ожидание загрузки**: Ждет полной загрузки DOM, всех ресурсов и завершения сетевых запросов
- **Поддержка JavaScript**: Использует реальный браузерный движок (Chromium)
- **Гибкая настройка**: Множество параметров для контроля процесса парсинга
- **JSON вывод**: Структурированный вывод с извлечением:
  - Заголовка и текста страницы
  - Всех ссылок
  - Форм и их полей
  - Изображений
  - Метаданных (cookies, viewport)
  - Полного HTML кода

## Требования

- Windows 10/11
- Python 3.8 или выше (только для сборки)

## Быстрый старт

### Вариант 1: Готовый EXE файл (рекомендуется)

1. Скачайте `web-parser.exe` из релизов
2. Запустите из командной строки:

```cmd
web-parser.exe https://example.com
```

### Вариант 2: Сборка из исходников

1. Клонируйте репозиторий или скачайте файлы
2. Запустите скрипт сборки:

```cmd
build.bat
```

3. После сборки используйте файл `dist\web-parser.exe`

### Вариант 3: Запуск из Python (для разработки)

```cmd
# Установите зависимости
pip install -r requirements.txt

# Установите браузеры Playwright
python -m playwright install chromium

# Запустите приложение
python web_parser.py https://example.com
```

## Использование

### Базовый синтаксис

```cmd
web-parser.exe URL [опции]
```

### Примеры использования

#### 1. Простой парсинг с выводом в консоль

```cmd
web-parser.exe https://example.com
```

#### 2. Сохранение результата в файл

```cmd
web-parser.exe https://example.com -o result.json
```

#### 3. Ожидание конкретного элемента

```cmd
web-parser.exe https://example.com --wait-selector "#content"
```

#### 4. Увеличенный таймаут для медленных сайтов

```cmd
web-parser.exe https://example.com --timeout 120
```

#### 5. Отладка с видимым окном браузера

```cmd
web-parser.exe https://example.com --visible
```

#### 6. Игнорирование SSL ошибок

```cmd
web-parser.exe https://example.com --ignore-https-errors
```

#### 7. Дополнительное время ожидания после загрузки

```cmd
web-parser.exe https://example.com --wait-time 5
```

#### 8. Логирование для отладки

```cmd
# DEBUG уровень - детальная информация
web-parser.exe https://example.com --log-level DEBUG

# Сохранение логов в файл
web-parser.exe https://example.com --log-file logs/parser.log

# Комбинация
web-parser.exe https://example.com --log-level DEBUG --log-file logs/debug.log
```

### Все параметры

| Параметр | Описание | Значение по умолчанию |
|----------|----------|----------------------|
| `url` | URL страницы для парсинга (обязательный) | - |
| `-o, --output` | Путь к файлу для сохранения результата | Вывод в консоль |
| `--wait-selector` | CSS селектор элемента для ожидания | - |
| `--timeout` | Таймаут загрузки в секундах | 60 |
| `--visible` | Показать окно браузера | false |
| `--ignore-https-errors` | Игнорировать SSL ошибки | false |
| `--wait-time` | Дополнительное время ожидания после загрузки (сек) | 2 |
| `--log-level` | Уровень логирования (DEBUG, INFO, WARNING, ERROR) | INFO |
| `--log-file` | Путь к файлу для сохранения логов | - |

## Формат вывода

Приложение возвращает JSON со следующей структурой:

```json
{
  "url": "https://example.com",
  "title": "Example Domain",
  "status_code": 200,
  "html": "<!DOCTYPE html>...",
  "text": "Example Domain\nThis domain is for use in...",
  "metadata": {
    "viewport": {"width": 1280, "height": 720},
    "cookies": [...]
  },
  "links": [
    {
      "href": "https://www.iana.org/domains/example",
      "text": "More information..."
    }
  ],
  "forms": [
    {
      "action": "https://example.com/search",
      "method": "get",
      "fields": [...]
    }
  ],
  "images": [
    {
      "src": "https://example.com/image.jpg",
      "alt": "Example Image"
    }
  ]
}
```

## 📝 Логирование и отладка

Приложение включает профессиональную систему логирования для мониторинга и отладки.

### Уровни логирования

- **DEBUG** - Детальная отладочная информация (разработка)
- **INFO** - Общая информация о ходе работы (по умолчанию)
- **WARNING** - Предупреждения
- **ERROR** - Ошибки с traceback
- **CRITICAL** - Критические ошибки

### Примеры использования

```cmd
# По умолчанию (INFO в консоль)
python web_parser.py https://example.com

# DEBUG режим для отладки
python web_parser.py https://example.com --log-level DEBUG

# Сохранение логов в файл (с автоматической ротацией)
python web_parser.py https://example.com --log-file logs/parser.log

# Максимальная детализация в файл
python web_parser.py https://example.com --log-level DEBUG --log-file logs/debug.log
```

### Что логируется

**DEBUG уровень:**
- Инициализация каждого компонента
- Параметры браузера и User-Agent
- Поэтапная загрузка (DOM → load → networkidle)
- Отпечатки найденных сертификатов
- Размеры данных и JSON

**INFO уровень:**
- Запуск и завершение операций
- Статус HTTP ответов и время загрузки
- Количество извлеченных элементов
- Успех/неудача парсинга

**WARNING/ERROR:**
- Предупреждения (сертификаты не найдены)
- Ошибки с полным traceback
- Таймауты и исключения

### Формат логов

```
2025-12-03 22:37:26 | INFO     | web_parser | main:473 | Запуск Web Parser
2025-12-03 22:37:34 | INFO     | parse_page:256 | Страница загружена, статус: 200, время: 3.52s
2025-12-03 22:37:40 | INFO     | parse_page:298 | Извлечено ссылок: 1
```

### Автоматическая ротация

Лог-файлы автоматически ротируются:
- Максимальный размер файла: 10 MB
- Количество файлов: 5
- Общий размер: до 50 MB

### Анализ логов

```cmd
# Поиск ошибок
grep "ERROR" logs/parser.log

# Время выполнения
grep "завершен за" logs/parser.log

# Статистика элементов
grep "Извлечено" logs/parser.log
```

**Подробное руководство:** [LOGGING_GUIDE.md](LOGGING_GUIDE.md)

---

## Работа с сертификатами

### Автоматический выбор сертификата

Приложение полностью автоматизирует работу с клиентскими сертификатами:

1. **Поиск сертификатов**: Автоматически ищет сертификаты в хранилище Windows `Cert:\CurrentUser\My`
2. **Автоматический выбор**: Использует флаги Chromium для автоматического выбора первого доступного сертификата
3. **CDP протокол**: Дополнительно использует Chrome DevTools Protocol для обработки диалогов выбора сертификата
4. **Без диалогов**: Диалоговое окно выбора сертификата **не появляется** - выбор происходит автоматически

**Технические детали:**
- Флаг `--auto-select-client-certificate` включает автоматический выбор
- Флаг `--enable-features=AutoSelectClientCertificateForUrls` активирует функцию для всех URL
- CDP обработчик перехватывает события `Security.certificateError` и автоматически продолжает работу

### Просмотр установленных сертификатов

```powershell
# В PowerShell
Get-ChildItem -Path Cert:\CurrentUser\My | Where-Object { $_.HasPrivateKey }
```

### Установка сертификата

1. Откройте файл `.pfx` или `.p12`
2. Выберите "Текущий пользователь"
3. Следуйте инструкциям мастера импорта
4. Сертификат будет доступен для приложения

## Устранение неполадок

### Приложение не находит сертификат

**Проблема**: Сообщение "Сертификаты не найдены"

**Решение**:
1. Проверьте наличие сертификатов в PowerShell (см. выше)
2. Убедитесь, что сертификат имеет приватный ключ
3. Проверьте, что сертификат не истёк

### Таймаут при загрузке страницы

**Проблема**: Ошибка timeout при загрузке

**Решение**:
```cmd
# Увеличьте таймаут
web-parser.exe https://example.com --timeout 120

# Добавьте дополнительное время ожидания
web-parser.exe https://example.com --wait-time 5
```

### Страница загружается, но данные неполные

**Проблема**: Отсутствует динамически загружаемый контент

**Решение**:
```cmd
# Дождитесь появления конкретного элемента
web-parser.exe https://example.com --wait-selector ".dynamic-content"

# Увеличьте время ожидания после загрузки
web-parser.exe https://example.com --wait-time 10
```

### SSL ошибки

**Проблема**: Ошибки сертификатов SSL

**Решение**:
```cmd
# Только для тестирования! Небезопасно для production
web-parser.exe https://example.com --ignore-https-errors
```

### Большой размер EXE файла

**Проблема**: web-parser.exe занимает много места

**Причина**: Включает полный браузерный движок Chromium

**Решение**: Это нормально. Standalone приложение без зависимостей должно включать браузер.

## Продвинутое использование

### Пакетная обработка нескольких URL

Создайте batch-скрипт `parse_multiple.bat`:

```batch
@echo off
for /f %%i in (urls.txt) do (
    echo Парсинг: %%i
    web-parser.exe %%i -o results\%%~nxi.json
)
```

### Интеграция с PowerShell

```powershell
# Парсинг и обработка результата
$result = & web-parser.exe https://example.com | ConvertFrom-Json

# Вывод заголовка
Write-Host "Title: $($result.title)"

# Количество ссылок
Write-Host "Links found: $($result.links.Count)"
```

### Использование в Python скриптах

```python
import subprocess
import json

# Запуск парсера
result = subprocess.run(
    ['web-parser.exe', 'https://example.com'],
    capture_output=True,
    text=True
)

# Обработка результата
data = json.loads(result.stdout)
print(f"Title: {data['title']}")
print(f"Links: {len(data['links'])}")
```

## Структура проекта

```
web-parser-app/
├── web_parser.py          # Основной исходный код
├── web_parser.spec        # Конфигурация PyInstaller
├── requirements.txt       # Python зависимости
├── build.bat             # Скрипт автоматической сборки
├── README.md             # Эта документация
└── dist/                 # Папка с собранным EXE (после сборки)
    └── web-parser.exe    # Готовое приложение
```

## Разработка

### Изменение кода

1. Отредактируйте `web_parser.py`
2. Протестируйте изменения:
   ```cmd
   python web_parser.py https://example.com --visible
   ```
3. Пересоберите EXE:
   ```cmd
   build.bat
   ```

### Кастомизация парсинга

Отредактируйте метод `parse_page` в `web_parser.py` для извлечения дополнительных данных:

```python
# Пример: извлечение всех заголовков
headers = self.page.eval_on_selector_all(
    'h1, h2, h3',
    '(elements) => elements.map(h => h.textContent)'
)
result['headers'] = headers
```

## Ограничения и известные проблемы

1. **Размер файла**: EXE файл большой (~150-200 MB) из-за встроенного браузера
2. **Производительность**: Первый запуск может быть медленным (инициализация браузера)
3. **Антивирусы**: Некоторые антивирусы могут ложно срабатывать на упакованные PyInstaller приложения
4. **CAPTCHA**: Приложение не может автоматически проходить CAPTCHA

## Безопасность

- ⚠️ **Не используйте** `--ignore-https-errors` в production
- ⚠️ **Проверяйте** сертификаты перед импортом в хранилище
- ⚠️ **Не передавайте** приватные ключи сертификатов третьим лицам
- ✅ **Используйте** только на доверенных сайтах

## Лицензия

MIT License

## Поддержка

При возникновении проблем:
1. Проверьте раздел "Устранение неполадок"
2. Запустите с параметром `--visible` для отладки
3. Проверьте вывод ошибок в stderr

## Альтернативы

Если это приложение не подходит для ваших задач:

- **curl/wget**: Для простых HTTP запросов без JavaScript
- **Selenium**: Для более сложной автоматизации браузера
- **requests + BeautifulSoup**: Для парсинга статических страниц
- **Scrapy**: Для масштабного веб-скрейпинга

---

**Версия**: 1.2.0
**Дата**: 2025-12-04

## История изменений

### Версия 1.2.0 (2025-12-04)
- ✅ **Улучшен автоматический выбор сертификатов**
  - Добавлен флаг `--enable-features=AutoSelectClientCertificateForUrls`
  - Реализован CDP обработчик для автоматического выбора сертификатов
  - Диалоговое окно выбора сертификата теперь не появляется
  - Логирование информации о используемом сертификате

### Версия 1.1.0 (2025-12-04)
- ✅ Добавлена профессиональная система логирования
- ✅ 5 уровней логирования (DEBUG, INFO, WARNING, ERROR, CRITICAL)
- ✅ Автоматическая ротация лог-файлов

### Версия 1.0.0 (2025-12-03)
- ✅ Первый релиз
- ✅ Автоматический выбор клиентских сертификатов Windows
- ✅ Умное ожидание загрузки страниц
- ✅ Вывод результатов в JSON формате
