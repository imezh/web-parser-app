# Web Parser - Быстрый старт

## 🚀 За 3 шага

### Шаг 1: Сборка приложения

```cmd
build.bat
```

Этот скрипт:
- Создаст виртуальное окружение Python
- Установит все зависимости
- Установит браузер Chromium
- Соберет standalone EXE файл

⏱ Время: ~5-10 минут (зависит от скорости интернета)

### Шаг 2: Проверка установки

```cmd
dist\web-parser.exe --help
```

Должна появиться справка по использованию.

### Шаг 3: Первый парсинг

```cmd
dist\web-parser.exe https://example.com
```

Результат будет выведен в консоль в формате JSON.

## 📋 Базовые примеры

### Сохранить результат в файл

```cmd
dist\web-parser.exe https://example.com -o result.json
```

### Увеличить таймаут для медленных сайтов

```cmd
dist\web-parser.exe https://slow-site.com --timeout 120
```

### Дождаться появления конкретного элемента

```cmd
dist\web-parser.exe https://example.com --wait-selector "#content"
```

### Показать окно браузера (для отладки)

```cmd
dist\web-parser.exe https://example.com --visible
```

## ⚙️ Системные требования

- Windows 10/11
- Python 3.8+ (только для сборки)
- ~500 MB свободного места на диске
- Интернет-соединение (для первой сборки)

## 🔧 Если что-то пошло не так

### Проблема: build.bat завершается с ошибкой

**Решение:**
1. Убедитесь, что Python установлен: `python --version`
2. Проверьте интернет-соединение
3. Запустите от имени администратора

### Проблема: "Файл заблокирован антивирусом"

**Решение:**
- Добавьте папку проекта в исключения антивируса
- PyInstaller иногда вызывает ложные срабатывания

### Проблема: Таймаут при парсинге

**Решение:**
```cmd
dist\web-parser.exe https://example.com --timeout 120 --wait-time 5
```

## 📚 Дополнительная документация

- [README.md](README.md) - Полная документация
- [INTEGRATION.md](INTEGRATION.md) - Примеры интеграции
- [example_usage.bat](example_usage.bat) - Интерактивные примеры

## 🎯 Типичные сценарии использования

### 1. Мониторинг изменений на сайте

```batch
:: Сохраняем текущую версию
dist\web-parser.exe https://example.com -o baseline.json

:: Через некоторое время проверяем
dist\web-parser.exe https://example.com -o current.json

:: Сравниваем файлы
fc baseline.json current.json
```

### 2. Извлечение данных из динамических страниц

```cmd
:: Ждем, пока JavaScript загрузит контент
dist\web-parser.exe https://spa-site.com --wait-selector ".loaded-content" --wait-time 5
```

### 3. Работа с сайтами, требующими сертификат

```cmd
:: Сертификат будет выбран автоматически из хранилища Windows
dist\web-parser.exe https://secure-site.com --timeout 90
```

### 4. Пакетная обработка списка URL

Создайте `urls.txt`:
```
https://site1.com
https://site2.com
https://site3.com
```

Создайте `parse_all.bat`:
```batch
@echo off
for /f %%i in (urls.txt) do (
    echo Parsing: %%i
    dist\web-parser.exe %%i -o results\%%~nxi.json
)
```

## ⚡ Советы по производительности

1. **Используйте headless режим** (по умолчанию) - быстрее
2. **Указывайте селектор** для ожидания конкретного элемента
3. **Оптимизируйте таймаут** - не ставьте слишком большой
4. **Кэшируйте результаты** если страница не меняется часто

## 🔐 Безопасность

- ✅ Сертификаты используются из системного хранилища Windows
- ⚠️ Не используйте `--ignore-https-errors` для важных сайтов
- ✅ Приложение не отправляет данные никуда кроме указанного URL
- ✅ Все данные остаются на вашем компьютере

## 📞 Получить помощь

1. Проверьте [README.md](README.md) - раздел "Устранение неполадок"
2. Запустите с параметром `--visible` для визуальной отладки
3. Проверьте вывод stderr на наличие ошибок

---

**Готово!** Теперь вы можете парсить веб-страницы с поддержкой сертификатов и динамического контента.

Для подробной информации см. [README.md](README.md)
